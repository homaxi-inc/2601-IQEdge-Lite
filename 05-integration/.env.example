# Victron VRM API v2 — copy to .env (gitignored). DO NOT COMMIT secrets.
# Source of truth for lab tokens: 003_IQTrailer/.env (research repo).
# Usage: Modbus/Cerbo bench cross-check only — NOT G2 production ingest (ADR-012).

VRM_API_BASE_URL=https://vrmapi.victronenergy.com/v2
VRM_ACCESS_TOKEN=
VRM_USER_ID=
VRM_X_AUTHORIZATION=Token <paste_token_here>
VRM_USERS_ME_URL=https://vrmapi.victronenergy.com/v2/users/me
VRM_USER_INSTALLATIONS_URL=https://vrmapi.victronenergy.com/v2/users/{idUser}/installations
